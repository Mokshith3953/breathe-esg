Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.4.0
dj-database-url==2.2.0
whitenoise==6.7.0
gunicorn==22.0.0
python-decouple==3.8
# psycopg2-binary is required only for PostgreSQL (production).
# No pre-built wheel for Python 3.14 yet; Render uses Python 3.11 where it works.
# Local dev uses SQLite (no psycopg2 needed).
